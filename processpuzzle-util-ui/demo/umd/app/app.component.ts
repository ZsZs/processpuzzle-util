import { Component } from '@angular/core';

@Component({
  selector: 'pp-app',
  template: `<pp-app-tick-tock></pp-app-tick-tock>`
})
export class AppComponent {
  public header: string = 'UMD Demo';
}
